<h1>Nie pamiętam hasła</h1>

  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :id => "flash_#{name}" %>
  <% end %>

<p>Proszę wprowadzić email służący do przesłania linku do resetowania hasła.</p>
<div class="row">
    <div class="col-md-6 col-md-offset-3">
		<%= form_tag password_resets_path do %>
		<%= label_tag :Email %><br />
		<%= text_field_tag :mail, nil, class: "form-control" %><br>
		<%= submit_tag "Wyślij email", class: "btn btn-primary" %>
		  </div>
	</div>
<% end %>